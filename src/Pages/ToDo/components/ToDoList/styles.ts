import React from "react";

const styles: { [key: string]: React.CSSProperties } = {
  list: {
    padding: "0px",
    overflowX: "hidden",
    overflowY: "auto",
    height: "auto",
    maxHeight: "250px",
  },
};
export default styles;
